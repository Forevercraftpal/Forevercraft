# Fishing Trial T8 — Standard lake (4 blocks deep), storm flavor
# Macro file — run with storage evercraft:trials

$execute positioned $(ax) 300 $(az) run fill ~2 ~ ~2 ~46 ~3 ~46 water

# Seagrass for visual variety
$execute positioned $(ax) 300 $(az) run fill ~10 ~ ~10 ~18 ~ ~18 seagrass replace water
$execute positioned $(ax) 300 $(az) run fill ~28 ~ ~28 ~38 ~ ~38 seagrass replace water
