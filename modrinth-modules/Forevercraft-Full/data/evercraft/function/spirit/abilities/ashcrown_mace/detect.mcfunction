# Ashcrown Mace — Detection Init (Sentinel)
# Run as @s when spirit weapon ID 7 is detected in mainhand
# Initialize shield charge counter for passive damage reduction stacking

scoreboard players set @s ec.sp_shield_charge 0
