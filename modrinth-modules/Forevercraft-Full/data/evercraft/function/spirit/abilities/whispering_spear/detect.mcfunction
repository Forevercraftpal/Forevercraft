# Thornmaw — Detection Init (Beastlord)
# Run as @s when spirit weapon ID 6 is detected in mainhand
# Passives: Pet stat doubling (handled by beastlord system), Prey Mark (25% on pet hit)
# Pet bonus tracking is managed by the companion handler — no per-detect init needed
