# Fishing Trial T2 — Standard lake (4 blocks deep)
# Macro file — run with storage evercraft:trials

$execute positioned $(ax) 300 $(az) run fill ~2 ~ ~2 ~46 ~3 ~46 water

# Scatter some seagrass
$execute positioned $(ax) 300 $(az) run fill ~5 ~ ~5 ~10 ~ ~10 seagrass replace water
$execute positioned $(ax) 300 $(az) run fill ~35 ~ ~35 ~40 ~ ~40 seagrass replace water
