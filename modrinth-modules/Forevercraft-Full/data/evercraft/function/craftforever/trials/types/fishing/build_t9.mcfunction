# Fishing Trial T9 — Deep lake (8 blocks deep)
# Macro file — run with storage evercraft:trials

$execute positioned $(ax) 300 $(az) run fill ~2 ~ ~2 ~46 ~7 ~46 water

# Kelp columns in deep water
$execute positioned $(ax) 300 $(az) run fill ~5 ~ ~5 ~10 ~5 ~10 kelp replace water
$execute positioned $(ax) 300 $(az) run fill ~35 ~ ~35 ~42 ~5 ~42 kelp replace water
$execute positioned $(ax) 300 $(az) run fill ~5 ~ ~35 ~10 ~5 ~42 kelp replace water
