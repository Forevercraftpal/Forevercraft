# Fishing Trial T5 — Standard lake (4 blocks deep)
# Macro file — run with storage evercraft:trials

$execute positioned $(ax) 300 $(az) run fill ~2 ~ ~2 ~46 ~3 ~46 water

# Kelp on bottom
$execute positioned $(ax) 300 $(az) run fill ~8 ~ ~8 ~14 ~ ~14 kelp replace water
$execute positioned $(ax) 300 $(az) run fill ~32 ~ ~32 ~38 ~ ~38 kelp replace water
