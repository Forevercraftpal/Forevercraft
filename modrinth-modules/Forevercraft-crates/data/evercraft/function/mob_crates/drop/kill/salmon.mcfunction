# Fishing Crate - salmon kill (uses fishing crate table, not mob crate)
advancement revoke @s only evercraft:mob_crates/kill/salmon
function evercraft:fishing/ref/check_crate

# Forever Coin — 1/2500 flat chance (no DR scaling)
function evercraft:coins/mob_drop
