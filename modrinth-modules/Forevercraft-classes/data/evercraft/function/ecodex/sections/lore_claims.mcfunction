# === ETERNAL CODEX — LORE CLAIMS (Section 15) ===
# 163 lore sets across 9 paginated pages
# Each completed set = 1 claimable reward (crate + coins based on rarity)
# Run as: player, at player

# Reset to page 1 on section entry
scoreboard players set @s ec.lr_page 0

# Spawn the first page
function evercraft:ecodex/sections/lore_claims/spawn_page
