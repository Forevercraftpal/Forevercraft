# Challenges Section — Click Detection (Section 2)
# Run as the player with menu open
# Challenges only (bestiary has its own standalone section 19)

# Page 1: Challenges
execute if score @s adv.gui_page matches 1 run function evercraft:codex/hub/challenges/check_clicks_page1
