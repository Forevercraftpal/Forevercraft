# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts
modrinth-modules/
build/

# Lockfiles — NOT committed: they pin the local-only @agency/ui-core path and
# break the Cloudflare build (forces strict `npm ci` against a missing local tgz).
# The last good production deploy committed no lockfile. Revisit once
# @agency/ui-core is published to a registry.
package-lock.json
bun.lock
